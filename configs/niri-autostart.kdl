// ============================================================================
// wayvid autostart configuration for niri
// ============================================================================
// Choose ONE of the methods below based on your preference
//
// RECOMMENDED: Use Method 1 (systemd add-wants) for proper session integration


// ----------------------------------------------------------------------------
// Method 1: systemd add-wants (RECOMMENDED for niri users)
// ----------------------------------------------------------------------------
// This is the recommended way according to niri documentation.
// Run this command ONCE in your terminal to set up wayvid with niri:
//
//   systemctl --user add-wants niri.service wayvid.service
//
// This creates a link in ~/.config/systemd/user/niri.service.wants/
// wayvid will automatically start with niri and stop when niri exits.
//
// Benefits:
// - Proper session lifecycle management
// - Easy to manage with systemctl commands
// - Clean start/stop with niri
// - View logs with: journalctl --user -u wayvid
//
// To remove: rm ~/.config/systemd/user/niri.service.wants/wayvid.service


// ----------------------------------------------------------------------------
// Method 2: spawn-at-startup (Simple, no systemd)
// ----------------------------------------------------------------------------
// Add this to your niri config.kdl if you prefer not using systemd:
// Note: Enable "Start minimized" in wayvid Settings to start in tray
//
// spawn-at-startup "wayvid-gui"


// ----------------------------------------------------------------------------
// Method 3: spawn-at-startup with systemctl (NOT recommended)
// ----------------------------------------------------------------------------
// This method works but is considered unconventional.
// Use Method 1 instead for better integration.
//
// spawn-at-startup "systemctl" "--user" "start" "wayvid.service"


// ----------------------------------------------------------------------------
// Method 4: With custom config file (Advanced)
// ----------------------------------------------------------------------------
// spawn-at-startup "wayvid-gui"


// ============================================================================
// Quick Setup Guide
// ============================================================================
// 1. Install wayvid (AUR: yay -S wayvid, or build from source)
//
// 2. Copy systemd service file (if not installed via package):
//    mkdir -p ~/.config/systemd/user
//    cp /path/to/wayvid/systemd/wayvid.service ~/.config/systemd/user/
//    systemctl --user daemon-reload
//
// 3. Enable wayvid to start with niri:
//    systemctl --user add-wants niri.service wayvid.service
//
// 4. Create config file:
//    mkdir -p ~/.config/wayvid
//    cat > ~/.config/wayvid/config.yaml << 'EOF'
//    source:
//      type: file
//      path: ~/Videos/wallpaper.mp4
//    layout: fill
//    volume: 0
//    EOF
//
// 5. Restart niri or run: systemctl --user start wayvid


// ============================================================================
// Useful Commands
// ============================================================================
// Check status:        systemctl --user status wayvid
// View logs:           journalctl --user -u wayvid -f
// Restart:             systemctl --user restart wayvid
// Stop:                systemctl --user stop wayvid
// Remove from niri:    rm ~/.config/systemd/user/niri.service.wants/wayvid.service
