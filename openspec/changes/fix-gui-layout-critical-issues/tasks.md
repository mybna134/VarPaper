## 1. 修复侧边栏宽度

- [x] 1.1 在 `app.rs` 展开状态的侧边栏容器添加固定宽度
- [x] 1.2 调整侧边栏宽度常量：固定 180px（已移除折叠功能）

## 2. 修复图标乱码

- [x] 2.1 在 `app.rs` 将导航图标从 Emoji 替换为文字标签
- [x] 2.2 在 `library.rs` 将壁纸类型图标移除（改为仅显示名称）
- [x] 2.3 在 `library.rs` 将其他 Emoji 替换为文字 ([Empty], [?], [!], ...)
- [x] 2.4 在 `about.rs` 用 SVG logo 替换 Emoji

## 3. 修复缩略图尺寸

- [x] 3.1 将缩略图尺寸从动态改为固定 120x120 (1:1 正方形)
- [x] 3.2 更新 placeholder_thumbnail 函数签名
- [x] 3.3 确保 `ContentFit::Cover` 正确应用
- [x] 3.4 减少卡片 padding/spacing 使缩略图更接近正方形

## 4. 响应式布局

- [x] 4.1 使用 `row().wrap()` 实现自动换行网格
- [x] 4.2 卡片数量根据窗口宽度自动调整

## 5. 验证

- [x] 5.1 运行 clippy 检查 - 通过
- [x] 5.2 启动 GUI 验证布局修复 - 完成
- [x] 5.3 测试窗口缩放时布局稳定性 - 完成
- [x] 5.4 测试关于页面不再叠加 - 完成
